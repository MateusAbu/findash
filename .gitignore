# Dependências
node_modules/

# Builds
dist/
build/

# Module Federation — cache do runtime (tipos @mf-types/ são VERSIONADOS:
# ADR-012 — typecheck em clone frio + contrato visível no diff de PR)
.mf/

# Ambiente
.env
.env.local
.env.*.local

# Ferramentas / SO
coverage/
.vercel/
.DS_Store
*.log
