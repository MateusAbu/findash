{
  "name": "findash",
  "private": true,
  "version": "0.0.0",
  "description": "Dashboard de finanças pessoais com microfrontends — Module Federation 2.0 + Rsbuild (projeto de estudo)",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.16.0",
  "scripts": {
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "prettier": "@findash/config/prettier",
  "devDependencies": {
    "@findash/config": "workspace:*"
  }
}
